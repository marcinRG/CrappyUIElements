import {IComponentProperties} from './IComponent.Properties';

export interface  IDirectionsRadioBtnsGroupProperties extends IComponentProperties {
    radioGroupName: string;
    radioButtonClass: string;
    radioWrapperClass: string;
}
